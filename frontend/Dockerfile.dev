# Helios Frontend — Desenvolvimento (hot-reload)
FROM node:24-alpine

WORKDIR /app

# Instalar dependências (--ignore-scripts: postinstall é desnecessário no container)
COPY package.json package-lock.json ./
RUN npm ci --ignore-scripts

# Copiar código fonte
COPY . .

# Angular dev server com host 0.0.0.0 (acessível fora do container)
EXPOSE 4200

CMD ["node", "scripts/load-env.mjs", "npx", "ng", "serve", "--host", "0.0.0.0", "--poll", "2000", "--proxy-config", "proxy.conf.docker.json"]
